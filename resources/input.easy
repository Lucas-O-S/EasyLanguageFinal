programa
numero i, soma;
booleano flag;

soma = 0;
flag = true;

para (i de 1 <= 10 passo 2) faca {
    escreva(i);
}

se (soma < 100 e flag == true) {
    escreva(soma);
} senao {
    escreva(0);
}

enquanto (soma < 100 ou flag == false) {
    soma = soma + 10;
}

fimprog;
