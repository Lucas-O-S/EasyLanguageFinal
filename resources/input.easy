programa

    numero i, soma;
    texto nome;

    leia(nome);
    escreva(nome);

    soma = 0;

    escreva(soma);
    
    enquanto (soma > 20) {
        soma = soma + 2;
        escreva(soma);
    }


fimprog;
